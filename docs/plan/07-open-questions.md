# 07 — Open Questions

> Everything the code could not answer, stated as a question with its impact, the phase it blocks, and the **default we proceed with** until answered. Nothing here is hidden as an assertion elsewhere in the plan.

| # | Question | Impact if unanswered | Blocks | Default while open |
|---|---|---|---|---|
| Q-1 | **When is Azion's Nuxt SSR adapter production-ready** (the `azion` bundler has `nuxt`/`nitro` presets; the edge function handler exists), and will webkit accept the SSR fixes (`dialog-content.vue:37`, `drawer-content.vue:43` top-level `document`)? | Determines *when* the SSR-ready SPA can flip specific routes to SSR/prerender; never blocks the migration itself | nothing now; a future rendering ADR | stay `ssr:false`; keep SSR-safety lint so the flip stays cheap |
| Q-2 | **v6 flag sunset timeline per domain** (`use_v6_configurations` at 100%? per-account rollout plan?) | The fork-sunset cutover rule gates every wave-B path that has a same-path fork | Phase 4 cutovers (not development) | build v6-only; cut over each path only on sunset confirmation |
| Q-3 | **Is console localization on the product roadmap?** | EN-only decision stands; if i18n is coming, string extraction should start before wave B volume | none now; retrofit cost grows per wave | English-only, no i18n layer |
| Q-4 | **Backend RBAC roadmap** (granular permissions via `identity-api` grants/groups): will the console need permission-aware UI beyond today's 2 checks? | `usePermission()` façade may need a real matrix + route meta | Phase 3+ IAM screens polish | thin façade over identity payload; backend 403 is the authority |
| Q-5 | **Who creates `azionapi-v4-typescript-sdk-dev`?** The processor's `generate.yml` matrix (`types: ["go"]`) is the designed extension point; adding `"typescript"` + a repo mirroring the Go workflow yields the SDK this plan's services layer is built to be replaced by | The disposal swap has no target until this exists | nothing (that's the point of the disposable layer) | `@console/services` impl/http IS the interim SDK; propose the matrix change to the API tooling team |
| Q-6 | **zod 4 upgrade trigger**: `@vee-validate/zod` peers `zod ^3.24` and webkit bundles zod 3 — when both move, do we? | Stuck on 3.25.x line meanwhile (fine; API stable) | none | zod 3.25.x pinned via pnpm catalog |
| Q-7 | **Real-Time Events v1 retirement**: can the v1 experience (and its persisted user preference toggle) be dropped so wave C ships only V2? | If v1 must live, wave C scope grows by a legacy screen set we planned to strand | Phase 5 cutover of `/real-time-events` | migrate V2 only; v1 users stay on old console until answered |
| Q-8 | **Edge decisioning for the kill-switch**: confirm the edge rules engine can route per-account (cookie/flag-based) for `console_next_optout`, and who owns writing that rule | Without it, rollback is all-accounts-per-route (still fine, coarser) | Phase 2 rollout polish | per-route cutover with all-accounts rollback; kill-switch added when confirmed |
| Q-9 | **Will the metrics/events GraphQL engines (beholder) get v4 REST equivalents?** `metrics-api.yaml` covers dashboards CRUD only | Determines whether `aux-metrics`/`aux-events` are permanent or future swap targets | none | treat as permanent aux (outside disposal boundary) |
| Q-10 | **Deployment/environment APIs** (`deployment-api.azion.app`, `environment-api.azion.app`) — will they join the v4 spec set (processor sources)? | `environments`/release services sit in aux; would move inside the disposal boundary | Phase 4 service shape (minor) | aux, hand-written |
| Q-11 | **Analytics vendor continuity**: Segment (+Appcues, HubSpot) stays through the migration? | Tracker facade port assumes same Segment payload shapes (parity checklists diff events) | Phase 1 analytics plugin | port Segment adapter as-is |
| Q-12 | **Dual-run freeze policy buy-in**: management agreement that a domain freezes in the old console when its wave starts | Without it, parity is a moving target and waves stall | every wave start | assume agreed; escalate at first violation |
| Q-13 | **Webkit upstream fixes** — file issues for findings from this discovery: SSR crashes (Q-1), `@modelcontextprotocol/sdk` as runtime dep (install weight for every consumer), missing `tailwindcss` peer declaration, `@aziontech/icons` broken `azionicons-color`/`color-catalog` exports (files absent from published dist), README documenting non-existent `@aziontech/theme/globals.css` + `tailwind-preset` subpaths, stale `@source` baked into theme dist | None block us (workarounds documented in `01-architecture.md` §3.10–3.11), but every future consumer trips on them | none | workarounds in place; issues to be filed at Phase 0 |
| Q-14 | **Sentry topology**: reuse the existing console Sentry project (keeps issue history/alert rules) or new project for the new app? | Alert rules, dashboards, release comparisons | Phase 1 Sentry setup | new project `console-kit-nuxt`, link old for reference |
| Q-15 | **Synthetic-check credentials**: vaulted test account for the authenticated prod probe (owner, rotation) | §7.5 synthetic journey limited to public probe until provided | Phase 2+ monitoring depth | public probe only |
| Q-16 | ⚠️ **Action, not question**: the old repo has a **committed `.env`** containing a live-looking `VITE_SENTRY_AUTH_TOKEN`, SSO IdP UUIDs and a Stripe test key. Rotate the Sentry token now; audit bucket/token scopes | Credential exposure predates this plan; cheap to close | immediate | reported here; owner needed |
| Q-17 | **Visual regression**: adopt webkit's Playwright+`jest-image-snapshot` approach for `@console/components`, or a hosted service? Interaction+a11y tests are already covered by addon-vitest either way | Screenshot-diff coverage for blocks | Phase 3+ (nice-to-have) | defer; stories+play-tests+a11y gate first |
| Q-18 | **Home dashboard scope**: migrate as-is or redesign (product may want new onboarding/widgets given everything else is new)? | Wave F scope for `/` | Phase 8 | parity first, redesign as follow-up |
